export type Position = {
  x: number
  y: number };

export type Dimensions = {
  width: number;
  height: number;
  depth: number;
};

export type Polygons = {
  topAndBottom: string;
  frontAndBack: string;
  leftAndRight: string;
}